---
title: 'Input Style'
---

## Field

<ResponseField name="field" type="object">
  An object that defines the style of the input element.

  <ResponseField name="styles" type="CSSProperties">
    CSS properties to apply to the input element.
  </ResponseField>

  <ResponseField name="className" type="string">
    Class name to apply to the input element.
  </ResponseField>
</ResponseField>

## Label

<ResponseField name="label" type="object">
  An object that defines the style of the input label.

  <ResponseField name="styles" type="CSSProperties">
    CSS properties to apply to the input element.
  </ResponseField>

  <ResponseField name="className" type="string">
    Class name to apply to the input element.
  </ResponseField>
</ResponseField>

## Error

<ResponseField name="error" type="object">
  An object that defines the style of the input error field.

  <ResponseField name="styles" type="CSSProperties">
    CSS properties to apply to the input element.
  </ResponseField>

  <ResponseField name="className" type="string">
    Class name to apply to the input element.
  </ResponseField>
</ResponseField>

## Container

<ResponseField name="container" type="object">
  An object that defines the style of the input container.

  <ResponseField name="styles" type="CSSProperties">
    CSS properties to apply to the input element.
  </ResponseField>

  <ResponseField name="className" type="string">
    Class name to apply to the input element.
  </ResponseField>

  <ResponseField name="focus" type="string">
    Class name to apply to the input element while input is focused.
  </ResponseField>
</ResponseField>
